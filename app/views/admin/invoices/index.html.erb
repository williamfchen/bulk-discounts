<center><h1>Invoices</h1></center>
<%= render partial: "admin/page_head" %>

<% @invoices.each do |invoice| %>
  <center><p>Invoice #<%= link_to "#{invoice.id}", admin_invoice_path(invoice.id) %></p></center>
<% end %>